1. **јвтообнуление default структур**  
   “ак как структуры автоматически инициализируютс€ (например, если поле не пришло), необходимо обнул€ть их вручную.

2. **ћетод получени€ статус-кода**  
   ѕолучение `Dt_Response_Code` и `HTTP_STATUS` в зависимости от коннектора.